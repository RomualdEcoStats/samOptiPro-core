% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnostics_by_target}
\alias{diagnostics_by_target}
\title{Target-level diagnostics (time + optional step-size proxy)}
\usage{
diagnostics_by_target(
  build_fn,
  opts = samOptiPro_options(),
  niter = opts$time_profile_n,
  samples = NULL
)
}
\arguments{
\item{build_fn}{A builder function returning \code{list(model=, cmodel=, monitors=?)}.}

\item{opts}{A list of options (e.g., from \code{samOptiPro_options()}).}

\item{niter}{Integer; iterations for time profiling (defaults to \code{opts$time_profile_n}).}

\item{samples}{Optional \code{coda::mcmc.list} used to compute the step proxy.}
}
\value{
A \code{data.frame} with columns \code{target}, \code{type}, \code{time_s}, \code{step_sd}.
}
\description{
Builds a robust MCMC configuration, profiles per-sampler time with
\code{\link{profile_sampler_times}}, maps samplers to their targets, and
optionally computes a step-size proxy per target via \code{\link{proxy_step_sd}}
if \code{samples} are provided.
}
\details{
Note: uses a tolerant default monitor set followed by a robust configuration
(no \code{monitors=} passed directly to \code{configureMCMC}).
}
\keyword{internal}
